export const escape=(v:string)=>v.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!));
const page=(body:string)=>new Response(`<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light"><title>Memory · 记忆库</title><link rel="stylesheet" href="/style.css"></head><body class="auth-body"><main class="auth"><a class="wordmark" href="/">M<span class="wordmark-dot">·</span> MEMORY</a>${body}<footer>保存原文，记得来处。</footer></main></body></html>`,{headers:{'content-type':'text/html; charset=utf-8'}});
export function loginPage(returnTo:string,error=''){return page(`<p class="eyebrow">PRIVATE MEMORY LIBRARY</p><h1>记忆，有迹可循。</h1><p class="muted">登录你的记忆库</p><form method="post" action="/login"><input type="hidden" name="return_to" value="${escape(returnTo)}"><label>用户名<input name="username" autocomplete="username" required maxlength="80"></label><label>密码<input name="password" type="password" autocomplete="current-password" required maxlength="128"></label>${error?`<p role="alert" class="error">${escape(error)}</p>`:''}<button class="primary" type="submit">登录 <span>↗</span></button></form>`);}
export function consentPage(client:string,scopes:string[],url:string,csrf:string){return page(`<p class="eyebrow">CONNECT YOUR ASSISTANT</p><h1>连接记忆工具</h1><p><strong>${escape(client)}</strong> 请求访问你的记忆库。</p><ul>${scopes.map(s=>`<li>${s==='memory:read'?'检索记忆、回查原文和版本':'保存记忆、创建纠正版本'}</li>`).join('')}</ul><p class="muted">连接后仍在原来的聊天中调用。可以随时在管理页撤销权限。</p><form method="post" action="/authorize/decision"><input type="hidden" name="authorize_url" value="${escape(url)}"><input type="hidden" name="csrf_token" value="${escape(csrf)}"><div class="actions"><button name="decision" value="deny">取消</button><button class="primary" name="decision" value="approve">允许连接 ↗</button></div></form>`);}
