# 实际验收记录

验收日期：2026-09-21（Asia/Shanghai）。测试仅使用明确标注的虚构纸船/梦境样本；没有读取或导入私人聊天记录。

## 发布

- 页面：https://memory.r-vera.com/
- MCP：https://memory.r-vera.com/mcp
- Cloudflare Worker：`vera-memory`
- 当前部署版本：`31727775-c62b-464c-9dc2-a796e94c5259`
- 初次部署版本：`75a087dd-992e-45b8-8527-3c3d1492c47b`
- 公网 `/health` 返回200，并实际访问页面、完成登录和 MCP 工具调用；不是只上传源码。

## 通过的检查

| 检查 | 本地 | 公网 HTTPS |
| --- | --- | --- |
| 未登录页面重定向到登录，API/MCP 拒绝未授权访问 | 通过 | 通过 |
| 浏览器登录、跨域写入拒绝、同源 CSRF 检查 | 通过 | 通过 |
| RFC OAuth metadata、规范 MCP resource | 通过 | 通过 |
| DCR → 登录同意 → PKCE S256 → access token | 通过 | 通过 |
| 授权码重放拒绝、错误 resource audience 拒绝 | 通过 | 通过 |
| 官方 MCP SDK 初始化、列出四个工具 | 通过 | 通过 |
| **页面保存 → MCP 搜索同一记录 → ID 回查原文** | 通过 | 通过 |
| **MCP 纠正 → 默认命中新版 → 旧原文及关系可回查** | 通过 | 通过 |
| 迟到纠正拒绝、梦境默认排除、无匹配明确返回 | 通过 | 通过 |
| 只读 OAuth 拒绝保存 | 通过 | 通过 |
| Refresh token 更新/轮转 | 通过 | 通过 |
| Bearer 服务令牌可用、撤销后拒绝 | 通过 | 通过 |
| OAuth 撤销后拒绝、退出后网页会话失效 | 通过 | 通过 |
| 桌面1440px、手机390px无横向溢出/页面 JS 错误 | 通过 | 通过 |
| 完整停止并启动本地 Worker，原文/版本仍在 D1 | 通过 | 不适用 |
| 发布新 Worker 版本后，线上两版原文及纠正关系仍在 | 不适用 | 通过 |

线上版本更新替换 Workers 执行代码；Cloudflare 没有可由应用操作的 Linux 进程“重启”按钮。因此明确区分**本地完整停服重启**与**线上重新部署跨版本持久化**，没有将其描述为重启现有 VPS。

自动化验收脚本每次包含17项协议/UI检查，本地另加停服启动持久化检查。独立核心/安全/适配器测试8项通过；原 Python 原型12项回归通过；TypeScript检查和生产打包通过。向量缓存/故障降级使用可控测试向量验证，未配置或声称验证真实外部嵌入模型。

截图在验收时查看过；虚构样本原文、来源、v1/v2关系与390px布局清楚可见。可用 `scripts/local-acceptance.mjs` 重现；截图和机器报告存放在未提交的 `artifacts/`。

## 清理和交付

线上两轮验收的6条虚构记录仅按报告中的精确ID清除，OAuth 测试授权及令牌已撤销。测试账户口令已替换为最终所有者账户；最终账户登录验证通过，旧测试凭据被拒绝，记录总数0、OAuth测试连接0、服务令牌0。初始登录信息只写入用户本机受保护文件，没有提交数据库、真实记忆或密钥。

## 明确未声称完成的事项

- 未代替用户在 Rowan 官端账号 UI 中安装连接；官端实际是否调用、何时调用仍由宿主控制。
- 已验证 Vesper 所需的 Streamable HTTP + OAuth/Bearer协议，并提供准确的源码接入点和服务端适配器；**未修改/部署 Vesper 每轮检索入口**，不能称为其每轮已自动使用本库。
- 没有真实语义模型质量评估、海量聊天导入或自动定时异地备份。
- GitHub发布流程提供手动部署入口，但生产凭据没有上传至仓库 Secrets；本次通过本机既有 Cloudflare 授权部署。
